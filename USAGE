usage: webapp <command> [<args>]

创建一个基于 backbone 的 web application

命令:
  new         <appName>           创建一个新的目录并且运行 npm install 和 npm start。
  generate    <blueprint> <name>  从模板生成代码
    alias: g
    可用的模板:
      view  <name>  生成 view。
      model <name>  生成 model。

示例:
  webapp new <appName Default: current directory>
    创建一个新的目录并且运行 npm install 和 npm start。

  webapp g view <viewName>
    生成 view。

  webapp g model <modelName>
    生成 model。
